#include <bits/stdc++.h>
using namespace std;

int main(void) 
{
    int t;
    scanf("%d", &t);

    while (t--)
    {
        int n;
        scanf("%d", &n);

        int max_queries = 2 * n;

        char o[n + 1];

        for (int i = 0; i < max_queries; i++)
        {
            printf("");

        }


    }
}